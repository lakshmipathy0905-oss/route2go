# Secrets — never commit real env files, only *.example
.env
.env.development
.env.staging
.env.production
**/firebase_options.dart.bak
**/google-services.json
**/GoogleService-Info.plist
**/*.jks
**/*.keystore
**/key.properties

# Flutter / Dart
**/build/
**/.dart_tool/
**/.flutter-plugins
**/.flutter-plugins-dependencies
**/.packages
**/pubspec.lock

# IDE
.idea/
.vscode/
*.iml

# iOS Swift Package Manager caches (build artifacts)
**/swiftpm/
**/Package.resolved

# OS
.DS_Store
Thumbs.db

# Supabase local dev
supabase/.branches
supabase/.temp

# k6 raw results (large; commit only the summary reports in docs/)
infra/load/k6/results/

# Local session tokens / helper scripts with live credentials (never commit)
infra/security/user_*.json
infra/security/get_token.sh
